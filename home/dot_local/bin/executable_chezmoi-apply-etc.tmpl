#!/usr/bin/env fish

{{ if eq .chezmoi.os "linux" -}}
set chezmoi_working_tree  (chezmoi execute-template "{{ .chezmoi.workingTree }}")
sudo chezmoi apply \
  --source "$chezmoi_working_tree/root" \
  --destination / \
  --mode symlink
{{ end }}
